#include <iostream>
#include <bits/stdc++.h>
#define forn(i, n) for (int i = 0; i < int(n); i++)

using namespace std;

int main(){
  int n;
  cin >> n;
  for(int i = 1; i <= n; i++) std::cout << i << ' ';
  return 0;
}